#include <stdio.h>

int main(){
    int num1, num2, num3;

    printf("Digite 3 valores inteiros: ");
    scanf("%d %d %d", &num1, &num2, &num3);   // Pode usar enter ou usar so espaços para salvar os numeros 
    
    printf("numeros:\nNumero 1: %d \nNumero 2: %d \nNumero 3: %d", num1, num2, num3);


return 0;
}